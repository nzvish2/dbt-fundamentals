version: 2

models:
  - name: dim_customer
    description: Customers Dimension
    columns: 
      - name: customer_id
        description: Unique Customer ID per row.
        tests:
          - unique
          - not_null
